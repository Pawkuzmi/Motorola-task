version: "2"
services:
  app:
    build: ./app
    links:
      - db
    ports:
      - "80:5000"

  db:
    image: mysql:8
    ports:
      - "33066:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - ./db:/docker-entrypoint-initdb.d/:ro